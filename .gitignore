# Downloaded source data and intermediate build products.
# Everything here is regenerable with `make all`.
raw/
build/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
env/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Local overrides
.env

# Uncomment if you would rather attach the GeoPackages to a Release than
# version them (they are 20 MB and 26 MB). `make all` regenerates both.
# data/*.gpkg
